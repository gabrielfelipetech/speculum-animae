[build]
  command = "npm ci --include=dev && (npm rebuild better-sqlite3 --build-from-source || npm i better-sqlite3 --build-from-source) && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
